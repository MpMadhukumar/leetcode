#include<stdio.h>
int stringlength( char str[])
{
    int length=0;
    while(str[length]!='\0')
    length++;
    return length ;
    
}
int main()
{
    char str[100];
    printf("enter the string:");
    fgets(str,100,stdin);
    printf("length:%d\n",stringlength(str));
    return 0;
}
