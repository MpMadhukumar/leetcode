#include<stdio.h>
int main()
{
    int num;
    printf("enter the integer:");
    scanf("%d",&num);
    if(num>0)
    {
        printf("the value is positive:");
    }
    else
    {
        printf("the value is negative :");
    }
    return 0;
}
